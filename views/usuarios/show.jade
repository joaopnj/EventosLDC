extends ../layout

block content
  h2 Visualizar usuários
  br
  form(method="GET", action="/usuarios/edit/#{value._id}", role="form")
    div(class="from-group")
      label Nome:
      input(type="text", name="nome", class="form-control", value="#{value.nome}", disabled="disabled")
      br
    div(class="from-group")
      label Login:
      input(type="text", name="login", class="form-control", value="#{value.login}", disabled="disabled")
      br
    div(class="form-group")
      label Id:
      input(type="text", name "senha", class="form-control", value="#{value._id}", disabled="disabled")
    div(class="form-group")
        label Data de cadastro:
        input(type="text", name "data", class="form-control", value="#{value.data_cad}", disabled="disabled")
    div(class="form-group")
      br
      a(href="/usuarios" title="Voltar",class="btn btn-info") Voltar
